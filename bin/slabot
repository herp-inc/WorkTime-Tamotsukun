#!/bin/bash

if [[ ! $SLACK_TOKEN ]]; then
   if [[ -e lib/secret ]]; then
       export SLACK_TOKEN=$(cat lib/secret)
   else
       echo 'you must set environment variable `SLACK_TOKEN`'
       exit 1
   fi
fi

node index.js
